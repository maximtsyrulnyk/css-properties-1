<div class="link_list">
    {{#each archive}}
    <strong class="link_title">{{title}}</strong>

    {{#each date}}
    <a href="{{url}}" class="link_item">{{text}}</a>
    {{/each}}
    {{/each}}
</div>